<div class="flex flex-row items-baseline py-2 px-4 bg-gray-100 border-b space-x-4">
    <%= render "shared/editable_label", 
        label: @board.title,
        label_class: "font-bold text-lg border border-transparent",
        editor_class: "font-bold text-lg",
        identifier: "board-title"
    %>
    <%= link_to "Back to workspace", 
        :workspace,
        class: "text-sm text-gray-500 hover:text-gray-800 transition-all" %>
</div>

<!-- board -->
<form data-controller="form-autosubmit" data-action="items:moved@window->form-autosubmit#submitEventData">
</form>
<%= render "shared/board_entries", board: @board %>
